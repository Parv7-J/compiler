proc foo[]{ //1
    proc bar[]{ //3
        proc baz[packed(Packed)]{ //7

        }
    }

    methods Packed { //4
        proc init[] {} //5

        proc foo[] { //6
            packing Packed { //8
                fields(arr[string]),
                values(arr[usize])
            }

            methods Packed { //9
                proc init[] {} //10
            }    
        }
    }
}

packing Packed { //2
    fields(arr[string]),
    values(arr[usize])
}
