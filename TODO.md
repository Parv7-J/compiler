### TODO:

1. add parsing synchronization 
2. improve lexer::produce_token 
3. improve lexer::StartedOperator
4. batch unknowns while lexing
5. add other tokens like % (modulo)
6. 'Not' operator can be logical or bitwise
7. 'usize' type is not machine dependent right now
8. make it a proper cli
9. methods in require and methods should get a 'this' symbol
10. init method should create a type
11. tuple types + hand initializing types 
12. enum variant getting using ::
13. collect all requires and (methods + its methods) first before attaching them
